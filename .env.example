# Optional. The dashboard works without a key (100 requests/min keyless, it uses 3).
# With a key the limit is 200/min. Copy this file to `.env` and paste your own key:
#
#   cp .env.example .env
#
# The extension never uses a key. `.env` is gitignored: never commit it.
WARERA_API_KEY=
