{
  "name": "warera-lens",
  "version": "1.4.0",
  "description": "Read-only WarEra equipment, case and travel insights with depth-aware quotes and explicit uncertainty.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "lint": "eslint extension scripts --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "validate": "node scripts/validate.mjs",
    "format": "prettier --write extension scripts README.md docs package.json tsconfig.json eslint.config.mjs vitest.config.js .github/workflows/ci.yml",
    "format:check": "prettier --check extension scripts README.md docs package.json tsconfig.json eslint.config.mjs vitest.config.js .github/workflows/ci.yml",
    "check": "npm run lint && npm run typecheck && npm run validate && npm test && npm run format:check"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "eslint": "10.10.0",
    "jsdom": "30.0.1",
    "prettier": "3.9.7",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
