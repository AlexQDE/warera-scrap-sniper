{
  "name": "warera-scrap-sniper",
  "version": "1.2.0",
  "description": "Chrome extension for WarEra: shows the scrap value of every gear offer on the equipment market and highlights the ones priced under it. Uses your own API key. Read-only, never buys.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
